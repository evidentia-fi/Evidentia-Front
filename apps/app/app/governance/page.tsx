import type { Metadata } from 'next';

import ComingSoon from '@workspace/ui/components/coming-soon';

export const metadata: Metadata = {
  title: 'Evidentia | Governance',
};

const Page = () => {
  return <ComingSoon />;
};

export default Page;
